function soma (x, y){
  return x + y;
}

const multiplicacao = function (x,y){
  return x * y;
}

const dividir = (x,y) => {
  return x / y;
}
 const objeto = () => ({nome: 'Vinicius', sobrenome: 'rebelo' });

 console.log(dividir(10,2));